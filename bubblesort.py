def bubble_sort(unsorted_list: list) -> list:
    """
    Sorts a list via bubble sort algorithm
    """
    for n in range(len(unsorted_list)):
        for element in unsorted_list:
            pass

        



def bubble_sort(unsorted_list):
    
    for i in range()